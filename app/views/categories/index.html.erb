<h1>All Categories</h1>

<div class="categories">
  <% @categories.each do |category| %>
    <h2><%= link_to category.name, category_path(category) %></h2>
    <p><%= link_to "Delete #{category.name}", admin_category_path(category), method: :delete %><p>
  <% end %>
</div>
